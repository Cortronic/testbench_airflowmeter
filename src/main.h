
typedef enum {
  MT_SELECT = 0,
  MT_SPEED = 1,
  MT_FLOW = 2,
  MT_POWER = 3,
  MT_CAL_FLOW = 4,
  MT_PID_TUNE_P = 5,
  MT_PID_TUNE_I = 6,
  MT_PID_TUNE_D = 7,
} ModeType;
extern ModeType modeType;
